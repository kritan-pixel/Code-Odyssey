<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Webpage</title>
    <style>
        /* Page Styling */
        body {
            margin: 0;
            padding: 0;
            background-color: #bfffcf; /* More visible light green background outside the rectangle */
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* Full viewport height */
        }

        .container {
            width: 90%; /* Wider rectangular area */
            max-width: 1200px; /* Prevent the rectangle from becoming too wide on large screens */
            background-color: #e6f7ff; /* Very very light blue shade for the container */
            border: 2px solid black;
            border-radius: 15px;
            padding: 20px;
        }

        .section {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .section:first-child {
            border-bottom: 2px solid #ccc; /* Separate the two sections */
            padding-bottom: 30px;
        }

        .logo {
            width: 150px;
            height: 150px;
            background-color: #ddd;
            border: 2px solid #bbb;
            border-radius: 50%; /* Circular placeholder for logo */
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden; /* Crop any overflowed image */
        }

        .logo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .search-bar {
            width: 60%;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 10px;
            background-color: #ffe6e6; /* Very very light red */
            text-align: center;
            transition: background-color 0.3s, border-color 0.3s; /* Smooth transition effect */
        }

        .search-bar:hover,
        .search-bar:focus {
            background-color: #ffb3b3; /* Softer, lighter red for hover/focus */
            border-color: #cc6666; /* Slightly darker border for subtle contrast */
            outline: none;
        }

        .icons-container {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            gap: 20px;
            width: 80%;
        }

        .icon {
            width: 80px;
            height: 80px;
            background-color: #ddd;
            border: 2px solid #bbb;
            border-radius: 15px; /* Slightly rounded corners */
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden; /* Crop any overflowed image */
            cursor: pointer;
        }

        .icon img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .icon:hover {
            background-color: #cce7ff; /* Light blue hover effect */
            border-color: #999;
        }

        .upload-input {
            display: none; /* Hide file input */
        }

        .upload-label {
            cursor: pointer;
            color: #777;
            font-size: 0.9rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- First Section: Logo and Search Bar -->
        <div class="section">
            <!-- Placeholder for Logo -->
            <div class="logo">
                <input type="file" class="upload-input" id="logo-upload" accept="image/*" onchange="previewImage(event, 'logo-preview')">
                <label for="logo-upload" class="upload-label">
                    <img id="logo-preview" src="" alt="Logo" />
                </label>
            </div>

            <!-- Search Bar -->
            <input
                type="text"
                class="search-bar"
                placeholder="Search here..."
            />
        </div>

        <!-- Second Section: Icons/Shortcuts -->
        <div class="section">
            <div class="icons-container">
                <!-- Placeholder for icons -->
                <div class="icon">
                    <input type="file" class="upload-input" id="icon1-upload" accept="image/*" onchange="previewImage(event, 'icon1-preview')">
                    <label for="icon1-upload" class="upload-label">
                        <img id="icon1-preview" src="" alt="Icon 1" />
                    </label>
                </div>
                <div class="icon">
                    <input type="file" class="upload-input" id="icon2-upload" accept="image/*" onchange="previewImage(event, 'icon2-preview')">
                    <label for="icon2-upload" class="upload-label">
                        <img id="icon2-preview" src="" alt="Icon 2" />
                    </label>
                </div>
                <div class="icon">
                    <input type="file" class="upload-input" id="icon3-upload" accept="image/*" onchange="previewImage(event, 'icon3-preview')">
                    <label for="icon3-upload" class="upload-label">
                        <img id="icon3-preview" src="" alt="Icon 3" />
                    </label>
                </div>
                <div class="icon">
                    <input type="file" class="upload-input" id="icon4-upload" accept="image/*" onchange="previewImage(event, 'icon4-preview')">
                    <label for="icon4-upload" class="upload-label">
                        <img id="icon4-preview" src="" alt="Icon 4" />
                    </label>
                </div>
                <div class="icon">
                    <input type="file" class="upload-input" id="icon5-upload" accept="image/*" onchange="previewImage(event, 'icon5-preview')">
                    <label for="icon5-upload" class="upload-label">
                        <img id="icon5-preview" src="" alt="Icon 5" />
                    </label>
                </div>
                <div class="icon">
                    <input type="file" class="upload-input" id="icon6-upload" accept="image/*" onchange="previewImage(event, 'icon6-preview')">
                    <label for="icon6-upload" class="upload-label">
                        <img id="icon6-preview" src="" alt="Icon 6" />
                    </label>
                </div>
            </div>
        </div>
    </div>

    <script>
        function previewImage(event, elementId) {
            const reader = new FileReader();
            reader.onload = () => {
                document.getElementById(elementId).src = reader.result;
            };
            reader.readAsDataURL(event.target.files[0]);
        }
    </script>
</body>
</html>
